import { useState } from "react";
import {
  SimpleGrid,
  VStack,
  Button,
  IconButton,
  Icon,
  useColorModeValue,
  Heading,
  HStack,
} from "@chakra-ui/react";

function Home(props) {
  return (
    <VStack>
      <Heading mt={10}>Home</Heading>
    </VStack>
  );
}

export default Home;
